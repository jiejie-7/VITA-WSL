1. 最致命的问题：PyTorch版本与多进程通信 Bug这份代码是基于 PyTorch 1.5 编写的，并使用了一个非常“黑科技”但也极其脆弱的多进程梯度同步 Trick。如果你现在使用的是较高版本的 PyTorch（比如 1.7 以上），这个通信机制就会完全失效。问题解析：打开 multi_processing.py 文件，找到 obtain_grad_pointers 函数：Pythondef obtain_grad_pointers(self):
    # only need perform this once
    if self.grads is None:
        ...
    if self.worker_grads is None:
        self.worker_grads = []
        for comm in self.comms:
            comm.send('send_grads')
            self.worker_grads.append(comm.recv())
作者为了图快，加了 # only need perform this once 的逻辑。在 PyTorch 1.5 (CPU) 中，通过 Pipe 发送过来的张量 p._grad.data 刚好可以利用底层共享内存，在 Worker 原地 zero_grad() 和 backward() 时，主进程里的张量也会跟着更新。但是，在高版本 PyTorch 中，这种隐式的梯度底层共享机制往往是阻断的。这意味着，除了第一个 Epoch 外，后续几百个 Epoch 中，主线程根本接收不到其他 15 个 Worker 算出来的真实梯度，只是在一遍遍累加第一轮的废弃梯度！后果： 你表面上开了 16 个进程（16倍吞吐量），但实际上模型几乎只靠主线程自己收集的一丁点数据在学习。对于 GRF 这种需要海量采样和探索的稀疏奖励环境，数据量少了 15/16，结果必然极差。修复建议：如果你不打算降级回 PyTorch 1.5，请修改 multi_processing.py 中的 train_batch 函数，强行在每个 Epoch 都获取新的梯度：Python# 注释掉 self.obtain_grad_pointers()，改为每次都显式拉取：
worker_grads = []
for comm in self.comms:
    comm.send('send_grads')
    worker_grads.append(comm.recv())

self.grads = []
for p in self.trainer.params:
    if p._grad is not None:
        self.grads.append(p._grad.data)

for i in range(len(self.grads)):
    for g in worker_grads:
        self.grads[i] += g[i]
    self.grads[i] /= stat['num_steps']
2. 环境版本完全不匹配 (Google Research Football)GRF 官方在过去几年里对环境有过大量更新（修复了许多 Bug，且强化了内置门将/防守 AI）。如果你是直接用 pip install gfootball 安装的最新版环境，你面对的 AI 对手比原论文作者跑的时候强了不止一个档次，且观测空间 (Observation) 的归一化尺度可能也有所不同。问题解析：原作者在 README.md 中明确指出他们 fork 了一个专属版本，并“added multi-agent observations and fixed some bugs”。如果你没有使用他们的特制环境，所有的动作逻辑和局部观测设定都会错位。修复建议：务必卸载现有的 gfootball，并按照 README 从作者的仓库安装：Bashgit clone https://github.com/chrisyrniu/football.git
cd football
pip install .
3. 注意 GRF 极度稀疏的奖励设定 (Sparse Reward)在这个仓库的设定中（train_grf.sh 中 --reward_type scoring），GRF 环境是一个极度稀疏的奖励环境：只有在进球时才给 +1 分，其余所有时刻全是 0 分。问题解析： 代码里并没有设置熵正则化（--entr 0）。在没有 Entropy Bonus 的情况下跑 PPO/A2C 这类算法，只要初始的几个 Epoch 探不出进球，策略就会迅速收敛到“原地发呆”或者“乱踢出界”的次优解，导致永远拿不到分。修复建议： 原论文给出的结果是跑了 5个不同的随机种子 (Seed) 之后平均出来的。单次运行（或者如果你正好固定了 Seed 为 0）有极高概率直接卡死在局部最优。如果你发现模型完全不学习，可以尝试增加少量熵正则化系数（比如在运行脚本里加上 --entr 0.001）来鼓励智能体探索，或者换几个不同的 --seed 多跑几次。4. GNN 层 Softmax 掩码的浮点隐患在 gnn_layers.py 中计算注意力机制时，作者并没有按照惯例用 -1e9 填充不需要通信的边（Masking），而是让 attention = e * adj 直接变成 0 然后送进 softmax。这导致不需要通信的节点在 Softmax 中贡献了 $e^0 = 1$ 的巨大分母，从而把有效的注意力权重过度稀释了。虽然作者在代码后面巧妙地用 self.normalize = True 强行除以总和把尺度救了回来，但引入了一个 1e-15 的极小项。虽然从数学结果上看勉强成立，但在复杂的网络前向和反向传播中，这种做法有概率引入不稳定的梯度。总结： 最核心的问题是环境版本不对以及高版本PyTorch引发的多进程梯度假死。优先修复这两点，你的学习曲线应该就能看到和原论文相似的上升趋势了。